# Backend config
PORT=3000
# MongoDB connection string used by the server (recommended env key: MONGO_URI)
# Example: mongodb+srv://user:pass@cluster0.mongodb.net/yourdb
MONGO_URI=mongodb+srv://uni804043_db_user:2124377as@cluster0.0cy1usa.mongodb.net/aalacomputer?retryWrites=true&w=majority
JWT_SECRET=replace_with_a_strong_secret
FRONTEND_ORIGIN=http://localhost:5173

# If your frontend is served from another domain, you can list allowed origins as
# a comma-separated list. Use '*' to allow any origin (not recommended for prod).
FRONTEND_ORIGINS=http://localhost:5173,https://aalacomputerkarachi.vercel.app,https://aalacomputer.com

# Vite/frontend env variables (exposed to browser when prefixed with VITE_)
# Set this to the backend URL your SPA should call (or leave unset to use relative '/api' in dev)
VITE_BACKEND_URL=http://localhost:3000
VITE_API_URL=http://localhost:3000
Pixabay_API_KEY=53093646-eebe1ceee2a14ccb43d033dae
# Example production frontend origins (add your deployed domains here or set
# FRONTEND_ORIGINS in your hosting environment). These are also included by
# default in the server helpers but can be overridden via env.
# FRONTEND_ORIGINS=https://aalacomputerkarachi.vercel.app,https://aalacomputer.com

# Google OAuth (create credentials at https://console.developers.google.com)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/v1/auth/google/callback